import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from scipy import stats
# Данные о продажах
data = {
 'SalesAmount': [100, 150, 200, 250, 300, 350, 400, 450, 500, 1000],
 'ProductCategory': ['A', 'B', 'A', 'C', 'B', 'A', 'C', 'B', 'A', 'C']
}
df = pd.DataFrame(data)
# Преобразуем столбец SalesAmount в вектор NumPy
sales_np = df['SalesAmount'].to_numpy()
# Применяем логарифмическое преобразование (бывает полезно для распределений со смещениями)
log_sales = np.log(sales_np)
# Рассчитываем базовые статистики
mean_sales = np.mean(sales_np)
median_sales = np.median(sales_np)
std_sales = np.std(sales_np)
# Вычисляем z-оценки
z_scores = stats.zscore(sales_np)
# Определяем выбросы (z-оценка > 3 или < -3)
outliers = np.abs(z_scores) > 3
# Выводим результаты
print("Исходные продажи:", sales_np)

print("Результат логарифмического преобразования:", log_sales)
print("Средние продажи:", mean_sales)
print("Медианные продажи:", median_sales)
print("Стандартное отклонение:", std_sales)
print("Z-оценки:", z_scores)
print("Выбросы:", df[outliers])
# Визуализируем результаты
plt.figure(figsize=(12, 6))
plt.subplot(121)
plt.hist(sales_np, bins=10, edgecolor='black')
plt.title('Распределение исходных продаж')
plt.xlabel('SalesAmount')
plt.ylabel('Частота')
plt.subplot(122)
plt.hist(log_sales, bins=10, edgecolor='black')
plt.title('Распределение логарифмированных продаж')
plt.xlabel('Log(SalesAmount)')
plt.ylabel('Частота')
plt.tight_layout()
plt.show()

"""Произведем разбор кода.
1. Подготовка данных:
• начинаем с импорта необходимых библиотек. В нашем случае это 
NumPy для числовых операций, Pandas для манипулирования данными, Matplotlib для визуализации и SciPy – для вычисления z-оценок;
• генерируем простые данные с категориями товаров и суммами продаж.
2. Преобразование данных:
• преобразуем столбец с именем SalesAmount в вектор NumPy. Это позволит выполнять математические и статистические операции над 
ним более эффективно.
3. Логарифмическое преобразование продаж:
• применяем логарифмическое преобразование к столбцу с продажами с помощью функции np.log().
4. Статистический анализ:
• с помощью соответствующих функций NumPy вычисляем основные 
статистические показатели продаж (среднее, медиану, стандартное 
отклонение);
• с помощью функции stats.zscore() из библиотеки SciPy рассчитываем z-оценки (z-score). Этот показатель говорит о том, на сколько 
стандартных отклонений значение отстоит от среднего;
• определяем выбросы в данных, полагая, что к ним относятся значения, для которых z-оценка превышает 3 по модулю.
5. Визуализация результатов:
• строим две гистограммы с помощью библиотеки Matplotlib: первая 
показывает распределение исходных данных, а вторая – преобразованных.

Этот простой пример демонстрирует всеобъемлющий подход к  анализу 
данных в миниатюре. Здесь мы выполнили преобразование исходных данных, вычислили необходимые статистические показатели, определили выбросы и построили две диаграммы. Вы увидели, что инструменты из библиотеки NumPy могут использоваться совместно с  Pandas, SciPy и  Matplotlib 
с целью проведения исследовательского анализа данных.

Логарифмическое преобразование часто используется в финансовом анализе и  моделях машинного обучения, поскольку позволяет избавиться от
смещенности распределений и тем самым подготовить данные в более нормализованном виде для некоторых типов анализа и моделирования.

"""