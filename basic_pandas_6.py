import pandas as pd
# Простой набор данных
data = {'Store': ['A', 'B', 'A', 'B', 'A', 'B'],
'Sales': [200, 220, 210, 250, 215, 240]}
df = pd.DataFrame(data)
# Группируем по Store и рассчитываем средние продажи по магазинам
avg_sales = df.groupby('Store')['Sales'].mean()
print(avg_sales)


"""
ПОСТРОЧНОЕ ОПИСАНИЕ СКРИПТА:

СТРОКА 1: import pandas as pd
Импорт библиотеки pandas и присвоение ей псевдонима pd для работы с DataFrame и структурами данных

СТРОКИ 3-4: data = {'Store': ['A', 'B', 'A', 'B', 'A', 'B'], 'Sales': [200, 220, 210, 250, 215, 240]}
Создание словаря data с двумя ключами:
- 'Store': список из 6 значений с названиями магазинов (A, B, A, B, A, B)
- 'Sales': список из 6 целых чисел - суммы продаж (200, 220, 210, 250, 215, 240)
Таким образом, имеем данные о продажах: для магазина A - 3 записи, для магазина B - 3 записи

СТРОКА 5: df = pd.DataFrame(data)
Преобразование словаря data в DataFrame pandas - создание таблицы с двумя колонками Store и Sales

СТРОКА 7: # Группируем по Store и рассчитываем средние продажи по магазинам
Комментарий, описывающий действие следующей строки: группировка данных по магазинам и вычисление среднего арифметического

СТРОКА 8: avg_sales = df.groupby('Store')['Sales'].mean()
Цепочка операций:
- df.groupby('Store') - группировка DataFrame по колонке 'Store' (создание групп A и B)
- ['Sales'] - выбор колонки 'Sales' внутри каждой группы
- .mean() - вычисление среднего арифметического для продаж в каждой группе
Результат (Series) сохраняется в переменную avg_sales:
  для магазина A: (200 + 210 + 215) / 3 = 208.333...
  для магазина B: (220 + 250 + 240) / 3 = 236.666...

СТРОКА 9: print(avg_sales)
Вывод на экран полученного результата avg_sales:
Store
A    208.333333
B    236.666667
Name: Sales, dtype: float64


Здесь мы вычисляем средние продажи в разрезе магазинов. Это важная, но достаточно простая операция. 
А что, если у вас достаточно большой датафрейм с  миллионами строк и  
вам необходимо выполнить более сложную группировку или отфильтровать данные с применением сложных критериев?
"""